# DEVLOG

## 2026-04-26

- Initialized PromptCraft as a TypeScript ESM npm package.
- Added pure core modules for parsing, rendering, validation, and combining prompt templates.
- Added a thin `promptcraft` CLI with `render`, `validate`, `combine`, and `inspect` commands.
- Added Vitest coverage for core behavior and CLI output paths.
- Documented v0.1.0 syntax, constraints, CLI usage, and future prompt-library roadmap.
